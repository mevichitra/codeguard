[build-system]
requires = ["setuptools>=70", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "codeguard-backend"
version = "0.1.0"
description = "CodeGuard AI Backend"
readme = "README.md"
authors = [ { name = "CodeGuard" } ]
license = { text = "Proprietary" }
requires-python = ">=3.12"

