CG-SEC-001 no longer reports a false positive for a multi-part concatenation of string literals such as `"SELECT " + " * " + " FROM t"`; the whole `+` tree is checked, not just the outermost operands.
