CG-SEC-005 now flags `os.system`, `os.popen`, `subprocess.getoutput`, and `subprocess.getstatusoutput` with a non-literal argument -- these always use a shell and have no `shell=` keyword, so they were previously missed entirely.
