CG-SEC-004 and CG-SEC-005 now resolve import aliases, so `from pickle import loads`, `import subprocess as sp`, and `from os import system as sh` are detected. A shared resolver (`codeguard.rules._pyimports`) handles this.
