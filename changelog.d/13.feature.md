Added a `.pre-commit-hooks.yaml` (hook ids `codeguard`, `codeguard-full`, `codeguard-ci`) and a composite GitHub Action at `action/` — `uses: mevichitra/codeguard/action@v2` — that runs `codeguard ci` and uploads SARIF to code scanning.
